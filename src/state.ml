type t = unit
(** The abstract type of values representing the game state. *)

let init_state = raise (Failure "unimplemented")
(***)

(**plays the card*)
let play = raise (Failure "unimplemented")

(**ends the turn*)
let end_turn = raise (Failure "unimplemented")

(**returns player hand*)
let checkhand = raise (Failure "unimplemented")
